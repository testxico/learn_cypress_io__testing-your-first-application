

Kickstart instructions: 

- Install cypress: 
`npm install cypress --save-dev`

- Create cypress project:
`npx cypress open`
then select E2E project, and accept default configuration

- Create first spect file, then open in editor and edit test accordingly
